# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.*
!.env.example

# Personal Claude Code overrides
.claude/settings.local.json

# Isolated agent worktrees (nested git checkouts, never meant to be committed)
.claude/worktrees/

# Local-only design reference notes (not shipped, not for the public repo)
reference/

# Supabase CLI local state (project link cache, not shared/committed)
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
