# Combat & Loot

> Part of the split `CLAUDE.md` docs — see [`CLAUDE.md`](CLAUDE.md) for git/versioning workflow and cross-cutting gotchas, including the `resolve-combat` duplication warning this file's math depends on.

## Core loop

**Melvor Idle-style menu-driven idle RPG**, superseding an earlier isometric-canvas action-combat build (Phaser, tile movement, manual attack-range/positioning). Melvor is the reference for *how* the game is presented (menu-driven, auto-idle combat, tabbed pages); Conquer Online remains the reference for *what* the game contains (classes, gear, gems, zone/monster names, etc.).

Combat is fully automatic: pick a zone, pick one monster from that zone's roster, hit Fight, and it resolves via a tick-gated resolver (`src/game/combat/`) — no movement, no manual attack timing, no positioning. A 2-hour-capped (scales with Prestige/account tier — see `CLAUDE.progression.md`) offline/idle-progress simulation (`src/game/combat/offlineProgress.ts`) reuses the same resolver logic to calculate rewards for whatever monster was last selected while away, applying rewards through the same code paths a live kill uses, with a summary modal (`OfflineProgressModal.tsx`) on return.

## Combat

- **Implemented**: `src/game/combat/combatResolver.ts` (pure rare-roll/HP/reward math), `useCombatStore.ts` (live tick-gated resolver), `CombatEngine.tsx` (non-visual, mounted unconditionally in `GameShell`, drives `runTick` off a 100ms `setInterval`). `runTick` gates each attack on `1000/attackSpeed`, requires Quiver for Hunter, resolves damage via `resolvePhysicalDamage`.
- Gold/EXP/item-drop grants happen instantly on kill. **Monster respawn gap (2026-08-17, requested by the user, replaces the original instant-respawn design)**: a fixed `RESPAWN_GAP_MS` (2s placeholder) now sits between a kill and the next monster appearing — mirrored in `useCombatStore.ts` (client, `respawnReadyAt` field — while waiting, `currentHp`/`maxHp` are 0, `runTick` skips both the player's attack and the monster's attack-back, same "neither side acts" shape the knockout timer uses; `CombatPage.tsx` shows a `RespawnBanner` in place of the portrait/HP bar) and `resolve-combat/index.ts` (server). The old instant-respawn model let a fast, overleveled character's attacks look like they landed more than once per attack-interval tick once a monster's remaining HP dropped low relative to hit damage — the gap fixes the visual pacing and also now caps real kill throughput regardless of character power (a one-shot kill still can't happen faster than once per `RESPAWN_GAP_MS`).
  - **Server reward math — cycle-time model, not a discrete-event walk**: unlike Row Combat (multiple simultaneous/switching targets, needed a full event walk), a single fixed target with a fixed gap reduces to simple closed-form math: `dps = hitChance × expectedDamagePerHit / attackIntervalMs`, `timeToKillMs = effectiveHp / dps`, `cycleTimeMs = timeToKillMs + RESPAWN_GAP_MS`, `expectedKillsThisWindow = elapsedMs / cycleTimeMs`. Replaces the old `totalAttacks × hitChance × expectedDamagePerHit` formula and its overkill cap entirely — kills are now bounded by wall-clock cycle time rather than raw damage output divided by HP, so the old `maxUsefulDamage` cap (and the bug it fixed, a level-100+ character idling a level-1 monster racking up 12,493 "kills") can't recur under this model. `totalExpectedDamage` (feeds `DAMAGE_EXP_SHARE`) is derived separately from the same cycle time, crediting only the post-gap "active" portion of any partial cycle. No new persisted state needed — purely a function of `elapsedMs`, same as before.
- **Rare monsters**: 5% chance/kill (`RARE_CHANCE`), 2× HP (`RARE_HP_MULTIPLIER`), 5× gold/EXP (`RARE_REWARD_MULTIPLIER`), all in `combatResolver.ts` (shared with offline simulator so odds never drift). Visual: pulsing glow (`.super-quality-glow`-style) on monster portrait/HP bar.
- **Damage formula**: `resolvePhysicalDamage`/`monsterDefense` — `attack - defense`, floored at 10% of `attack` (`MIN_DAMAGE_PERCENT_OF_ATTACK`). `attack = physicalAttack + magicAttack`. Player's own Defense is modeled: monster-attack-back uses `resolvePhysicalDamage(monsterAttackDamage(type), effectivePlayerDefense)`.
  - **Dodge/hit-chance**: `rollIsHit(dodge)` — incoming evasion, PLACEHOLDER `DODGE_CHANCE_PER_POINT` capped at 50% (`MAX_DODGE_CHANCE`). `rollAttackLands(playerDexterity, monsterDodge)` — outgoing hit chance, mirrors `rollIsHit`, sharing the same per-point/cap constants. **Dodge and Dexterity are separate derived stats**: `dodge = Agility×1 + Boots' own dodge base_stats key` (feeds `rollIsHit`, incoming only). `dexterity = Agility×1 + Bows'/Rings' own dexterity base_stats key` (feeds `rollAttackLands`, outgoing only). `monsterDodge(type) = Math.round(level * 0.8)` (formula-derived, not stored, unsourced placeholder pacing). Both incoming (client-only, `useCombatStore.runTick`) and outgoing (mirrored in `resolve-combat/index.ts`, since it affects real kills) paths wired in. **`DODGE_CHANCE_PER_POINT` lowered 2026-08-11 from 0.005 (0.5%/point) to 0.0015 (0.15%/point)**, confirmed with the user as "way too high" — Hunter is the Agility specialist and its Agility alone crosses 100 by around level 45 (`classes.ts`'s `ATTRIBUTE_ANCHORS`), which saturated the 50% cap on its own from that level on, before any Boots/composition dodge bonus. The new rate means only a true best-in-slot level-130 Hunter (Agility 275 + Ascended Boots' dodge, ~341 points total) approaches the 50% cap. Since the constant is shared with `rollAttackLands`, this also makes the player's own outgoing attacks whiff more often on a monster whose Dodge outpaces their Dexterity — a deliberate, symmetric change, not incoming-only. A **"Miss" floating combat text** (added same day) now shows for both directions — `CombatLogKind`'s existing `'miss'`/`'dodge'` entries (previously logged with no visible floating indicator) render "Miss" in `CombatPage.tsx`'s floating-number blocks (`text-slate-300`, distinct from the amber/rose damage-number color) alongside the existing "-N" damage numbers, reusing the same `floatingNumbers`/`playerFloatingNumbers` derivation and `AnimatePresence` blocks (both desktop and mobile layouts).
  - **Min/max hit range**: attack rolls around the stored stat value (`rollDamageInRange`/`damageRangeFromMidpoint` in `combatResolver.ts`) — min = mid×`DAMAGE_ROLL_MIN_RATIO`, max = mid×`DAMAGE_ROLL_MAX_RATIO`. Narrowed 2026-08-11 from 0.5/1.5 (±50%, a 3x ratio) to **0.9/1.1** (a 1.222x ratio) — the original ±50% spread was only validated against the level-1 Iron Ring's tiny-integer rounding (min 1/max 3 at mid 2), but the fuller `reference/conquer-items/bows.md`/`rings.md` tables show real Bows running a much tighter ~1.2-1.33x ratio at nearly every level; 0.9/1.1 was picked to match Bows specifically since Hunter/Bow is the only playable class+weapon today (Rings run wider in the real data, ~1.8-2.5x through most levels, only converging to 1.5x at the top few tiers — a known, accepted under-fit since this is one shared constant across every `physical_attack`/`magic_attack` roll). Mirrored in `resolve-combat` (rolled fresh per attack in the simulation loop). Gear tooltips show `min–max` for `physical_attack`/`magic_attack` (via `formatBaseStats`, same constants — so tooltip and combat rolls always match); `StatsPanel.tsx`'s aggregate rows stay flat totals (combining two stats before ranging would mislead).
- **Level-diff color + EXP scaling**: `getLevelDiffColor`/`expMultiplierForLevelDiff` in `combatResolver.ts`. `diff = characterLevel - monsterLevel`. White (-2..2, EXP×1), Green (≥3, EXP×0.5), Red (-3/-4, EXP×1.5), Black (≤-5, EXP×2). Applies identically live and offline. "Black" renders as the darkest still-legible shade (not literal black) on this dark theme.
- **Level-gap Defense debuff**: two separate, asymmetric per-color multiplier tables. **Outgoing** (player hits monster) `MONSTER_DEFENSE_MULTIPLIER_BY_COLOR`: Green ×0.5, White ×0.75, Red/Black ×1 (unaffected). **Incoming** (monster hits player) `PLAYER_DEFENSE_MULTIPLIER_BY_COLOR`: Red ×0.5, Black ×0.1, White/Green ×1 (unaffected) — steepened 2026-08-07 (was Red ×0.75/Black ×0.5, "not a significant enough reduction") so a mismatched fight hits noticeably harder; may need a per-class pass later. `monsterDefense(type, characterLevel)` (mirrored in `resolve-combat/index.ts`, outgoing side only — affects real kills) applies the outgoing multiplier on top of `Math.round(level * 1.5)` base. Incoming side (`effectivePlayerDefense`) is computed inline in `useCombatStore.runTick`, **client-only, not mirrored server-side** (player HP has never been simulated server-side). Only Defense/mitigation is affected — dodge/hit-chance untouched.
- **Monster EXP reward** is formula-derived from monster level, not a stored field (`expRewardForLevel` in `src/game/stats/expCurve.ts`, mirrored in `resolve-combat/index.ts`; `enemy_types.exp_reward` column left in place but unread). `expRewardForLevel(level) = requiredExpForLevel(level) / killsPerLevelForLevel(level)`. Kills-per-level steps up at each promotion tier (1/15/40/70/100/110/120): `PROMOTION_TIER_ANCHORS`/`KILLS_PER_LEVEL_BY_TIER = [200, 350, 650, 1300, 2600, 5200, 10000]` (~10 min/level early → ~8.3 hr/level at tier 7). PLACEHOLDER table. Known minor quirk: EXP reward can dip slightly right at a tier boundary (not fixed, cosmetic).
  - **Damage-dealt EXP**: `DAMAGE_EXP_SHARE` (0.5, 50%) of the target's `expRewardForLevel`, split proportionally by HP-fraction-of-damage-dealt — sums to ~50% of base reward across a full kill, on top of the full on-kill grant (so a full kill nets 150%). Gets the same level-diff multiplier and a rare-blended multiplier as kill EXP (see the expected-value rewrite below). The old per-hit `damageExpForHit(damage, monsterMaxHp, monsterLevel)` helper was **removed** 2026-08-11 (nothing called it anymore) — reward math in both `resolve-combat` and the client's own prediction (`combatResolver.ts`'s `expectedRewardPerAttack`) now compute this as one continuous closed-form total over the whole window using the exported `DAMAGE_EXP_SHARE` constant directly (`expCurve.ts`), not per-hit — see the "Expected-value reward math" bullet further down. The live visual damage log line (`useCombatStore.runTick`) shows only the damage number, no EXP value.
  - **AFK-cap tier reward**: the bounded elapsed-time window `resolve-combat` will simulate scales with the highest **account-wide Achievements tier** reached on any monster (`account_monster_kills.claimed_tier_index`, `AFK_CAP_MS_BY_ACCOUNT_TIER`) — tier 0 = 2 hours up to tier 6 = 12 hours (2/3/4/6/8/10/12). PLACEHOLDER table. Server-only, no client mirror. (Originally keyed off the per-character Prestige/Kill-Count track; rebased onto the account track since the old one was too trivially reachable.)
  - **Idle/offline EXP rate**: `IDLE_EXP_MULTIPLIER = 0.5` in `resolve-combat/index.ts` — offline catch-up (`mode: 'offline'`) earns EXP at half the live rate, applied once to the window total. Gold unaffected. Server-only.
  - **Death timer**: on player knockout (HP 0), `useCombatStore.reviveAt` locks both sides for `KNOCKOUT_LOCKOUT_MS` (10s placeholder) — `isFighting` stays true, monster HP is NOT reset, fight resumes exactly where it was; player revives to full HP. `start()` clears stale `reviveAt`. `KnockoutHud.tsx` (in `GameShell`'s top HUD strip, alongside `QuiverWarningHud`/`InventoryFullWarningHud`) shows a live "Knocked out — reviving in Ns" countdown while `reviveAt > 0` — added 2026-08-07, the only prior signal was a Combat Log line, easy to miss since the log is collapsible. **Client-only, live combat only** — not mirrored server-side (player HP/death has never been simulated in `resolve-combat`), so offline/idle resolution still doesn't stop a player from idling a monster far above what they could survive live — a known, disclosed gap.
  - Deferred, not built: gating which monsters can be idled/AFK'd (e.g. only after a "Boss" kill — no Boss concept exists); a hard level-gate on selectable monsters.
- **Player HP**: `useCombatStore.currentPlayerHp`/`maxPlayerHp` (from `derivedStats.hp`), continuous across respawns/zone switches (not reset by `start()`/`stop()`/`clear()`, only by knockout). Shown as its own HP bar above the monster card, labeled with character name. Monster attacks on a placeholder flat `attackDamage` per `EnemyTypeDef`, on a fixed cadence (`MONSTER_ATTACK_INTERVAL_MS`, once/sec placeholder) — independent of rare HP/reward scaling. **Known gap**: `offlineProgress.ts` doesn't simulate monster attack-back at all — offline character is effectively invulnerable, a disclosed inconsistency with live combat.
- **Live combat stops on a full Inventory**: `resolveCombat()` takes `mode: 'live' | 'offline'`. Live mode stops simulating the rest of the window the moment a drop can't fit (kill's own gold/EXP already counted) and returns `inventoryFull: true`, never touching `loot_holding`. Offline mode always routes drops to Loot Holding regardless of room (see Loot below). Client: `useCombatStore.stopForInventoryFull()` (sets `isFighting: false`, red `'inventory-full'` log line); `InventoryFullWarningHud.tsx` (in `GameShell`'s top HUD) shows a steady amber "Inventory full" banner reactively, flashing red when a live resolve halts the fight.

## Zones

Zones are menu groupings, not spatial maps: pick a zone, see its 5-monster roster, pick **one** to fight continuously. All **8 zones implemented**, none locked, in `src/game/zones/zoneData.ts`: **Windhollow** (L1–25), **Cinderleaf** (L27–45), **Stormvale** (L47–65), **Sunscar Wastes** (L67–85), **Talon Isle** (L87–100), **Duskspire Keep** (L102–120), **Twistpath Ruins** (L105–120), **Rimehollow** (L120–129). Names/numbers are original designs, only pacing patterns were studied from real Conquer data (kept local-only in `reference/conquer-items/monsters.md`, not shipped). Every `EnemyTypeDef` carries its own `level`; stats (`maxHp`/`goldReward`/`expReward`/`attackDamage`) are independently formula-derived, tied to the Bow gear curve (`maxHp` ≈ 8× a same-level bow's damage).

Rosters (5 monsters each, first/last always kept, middle 3 spaced evenly by level):
- Windhollow: Quailwing(1), Mourning Dove(7), Redbreast(12), Warshade(20), Grim Specter(25)
- Cinderleaf: Wingfang Serpent(27), Brushrunner(32), Thornreaver(35), Woodkin(40), Woodkin Sovereign(45)
- Stormvale: Ridgeback Simian(47), Boulder Ape(52), Bellowing Brute(55), Frostpelt(60), Venomkin(65)
- Sunscar Wastes: Dunecrawler(67), Cragbeast(72), Boulderback Golem(75), Stonewarden(80), Edgeborn(85)
- Talon Isle: Wingkin(87), Wingkin Sovereign(90), Hawklord(92), Silverwing(95), Footpad(100)
- Duskspire Keep: Cryptwing(102), Crimson Wing(107), Crimson Sovereign(110), Ironhorn Fiend(115), Verdant Fiend(120)
- Twistpath Ruins: Hollow Sentinel(105), Gilded Wraith(108), Mosswarden(112), Nightfiend(117), Bullhorn Warden(120) — renamed 2026-08-13 from Ratling Flinger/Swiftgnaw (cosmetic only, stable ids `ratling-flinger`/`swiftgnaw` and all stats unchanged)
- Rimehollow: Rime Serpent(120), Serpent Herald(122), Serpent Warden(124), Fiend Sovereign(127), Frostblade Fiend(129)

No HP/gold/EXP rebalancing was needed for the roster trim (each monster's stats are hand-placed per its own level, not interpolated). Removed monster ids no longer exist at all (`delete from enemy_types where id in (...)`, migration `20260731110000_trim_enemy_rosters.sql`) — `selected_monster_id` has no FK, so a returning character with a removed selection just falls back to "pick a monster."

- **Zone/monster picker UI**: one combined "Zone & Monster" card (`CombatPage.tsx`), two `<select>`s + Fight button. Locked/empty options show disabled, not filtered. Picking a monster only stages a selection (`useZoneStore.selectedMonsterId`); Fight commits. Options show `(Lv N)`/`(Lv min-max)` and level-diff coloring (`LEVEL_DIFF_HEX_COLOR` inline hex map, since `<option>` doesn't reliably respect Tailwind classes).
- HP/gold/EXP per monster and gear level per zone are **unresolved placeholders — needs deliberate design, not sourced**.
- **Real portrait art**: all 40 monsters across all 8 zones have real art (2026-08-15, Rimehollow's 5 were last) via `EnemyTypeDef.portraitUrl` (`public/monsters/<id>.png`) — the `hexColor(activeType.color)` swatch fallback path is now dead for every current monster, kept only for whenever a new one is added without art yet. Sizing: `object-contain p-[15%]` (inset with padding, not edge-to-edge), no fixed source resolution required (existing files range ~400-1000px per edge, capped at 1000px on the longest edge, same sharp pipeline as nav icons minus the square crop). Add more art by dropping a PNG + setting `portraitUrl`.
- Switching zones (`useZoneStore.setCurrentZoneId`) always clears selected monster and fully resets combat state (`useCombatStore.clear()`).

## Loot

- Gear drop-rate *template* weighting (which template drops, beyond drop-chance-at-all) is **unresolved — needs deliberate design, not sourced**. Comet/Fallen Star rates are resolved (see `CLAUDE.progression.md`).
- Rare-monster status affects the existing roll rather than being a separate roll.
- **Server-authoritative combat resolution**: `supabase/functions/resolve-combat/index.ts` (Edge Function, invoked via `resolveCombat()` in `src/game/combat/resolveCombat.ts`) is the **sole** grantor of gold/EXP/item-drops/currency-drops for combat. It independently re-derives everything from the DB (never trusts client params): monster from `characters.selected_monster_id`/`current_zone`, combat stats from class+equipment read fresh, monster stats from `enemy_types` (server's own copy of `zoneData.ts` — regenerate if that file changes), elapsed time from `characters.combat_last_resolved_at timestamptz` (server clock, capped per the AFK-tier table above).
  - **Known, disclosed duplication — "must stay in sync" landmine**: the Edge Function's math (HP/reward scaling, EXP curve, damage formula, currency odds) is a deliberate copy of `combatResolver.ts`/`derivedStats.ts`/`classes.ts`/`equipmentBonus.ts`/`useProgressionStore.ts` (Deno can't import Vite-resolved TS directly). Mirror any change to those files into the Edge Function too.
  - `offlineProgress.ts` is now a thin wrapper calling `resolveCombat` once at login; shows the summary modal only if elapsed window ≥ `OFFLINE_SUMMARY_THRESHOLD_MS` (60s).
  - **Cadence**: client's 100ms `runTick` loop (HP bars, pacing, animation, log flavor) stays instant/unchanged and grants nothing real. `CombatEngine.tsx` calls `resolveCombat` on a ~4s interval (`RESOLVE_INTERVAL_MS`, shortened from 15s) while fighting, plus immediately on stop/switch/`visibilitychange`/`beforeunload`. Item/currency-drop combat-log lines between reconciliations are **PREDICTIVE ONLY**, corrected on the next response (unchanged) — gold/EXP/kill-count are no longer in this "tolerate divergence" bucket, see below.
  - **Drop-pool caching (2026-08-13, PostgREST egress fix)**: the level-appropriate drop-template query (`item_templates`, ~110 rows) used to re-run every single resolve call — at the ~4s cadence above across every active session, this was by far the largest driver of PostgREST egress against the free tier. `getDropPool(db)` now caches it in the Edge Function's own module scope (a plain top-level variable, which Deno keeps warm across invocations on the same instance) with a 10-minute TTL (`DROP_POOL_CACHE_TTL_MS`). **Gotcha**: editing the gear catalog (`item_templates`) won't be reflected in live drop selection for up to 10 minutes on an already-warm instance — redeploying the function (`supabase functions deploy resolve-combat`) resets it immediately via a cold start. Ownership/cost/RNG are untouched by this — only which template a drop rolls can lag.
  - **Expected-value reward math (2026-08-11 rewrite)** — replaced per-attack RNG simulation for gold/EXP/kill-count with deterministic closed-form math on both sides, specifically to fix a recurring bug: two independent RNG simulations of the same resolve window (client's live tick loop vs. the server's own batch simulation) could disagree by a kill or two, which the old 5%-shave safety margin didn't reliably absorb, especially once composition/dodge/damage-ratio tuning changes widened the gap further. Now both sides compute `hitChance` (deterministic, no roll — `1 - min(max(0, monsterDodge - dexterity) * DODGE_CHANCE_PER_POINT, MAX_DODGE_CHANCE)`) and `expectedDamagePerHit = resolvePhysicalDamage(attackMidpoint, monsterDefense(...))` directly off the stored midpoint stat (no `rollDamageInRange` involved), then `expectedKills = totalAttacks * hitChance * expectedDamagePerHit / (monster.maxHp * RARE_BLENDED_HP_FACTOR)`, with a **overkill cap** (fixed 2026-08-09, reported by the user — a level-100+ character idling a trivial level-1 monster for 19 real minutes came back to 12,493 "kills"): the raw `totalAttacks * hitChance * expectedDamagePerHit` damage figure is clamped to `totalAttacks * hitChance * monster.maxHp * RARE_BLENDED_HP_FACTOR` *before* dividing, since one landed attack can only ever finish off the one monster in front of it — a hit's damage past that monster's max_hp is wasted, not carried into the next spawn, so nothing should let kills exceed `totalAttacks * hitChance`. Capping the damage input (rather than kills directly) keeps `damageExp`, which is computed from the same damage figure, capped in the same proportion for free. Client mirror (`combatResolver.ts`'s `expectedRewardPerAttack`) applies the equivalent per-single-attack cap (`hitChance * type.maxHp * RARE_BLENDED_HP_FACTOR`, since each call there represents exactly one attack, not a window) — keep both in sync. **Rare-monster status is a blended expected-value multiplier for reward math** (`RARE_BLENDED_HP_FACTOR = 1.05`, `RARE_BLENDED_REWARD_FACTOR = 1.2`, `RARE_BLENDED_DAMAGE_EXP_FACTOR = 1.075`, each `= (1-RARE_CHANCE) + RARE_CHANCE * <rare's own multiplier>`) rather than tied to any individual roll — tying it to one roll would reintroduce the same per-window divergence, worse (a 5x swing). The **visual** rare-spawn roll (glow, toast, "A rare X appears!", 2x HP monster instance, `rollIsRare()`) is untouched, purely cosmetic now. Client mirror: `combatResolver.ts`'s `expectedRewardPerAttack(attackMidpoint, dexterity, type, characterLevel)`, called once per attack-interval tick in `useCombatStore.runTick` **unconditionally** — before the `rollAttackLands` miss-check, not gated behind it, since the expected value already bakes in hit-chance (gating it behind the visual roll would double-apply that chance and under-predict). The visual log/floating-number layer (`rollAttackLands`, `rollDamageInRange`, "Miss" text, kill log lines with `killRewards`' still-random flavor numbers) is completely unchanged and no longer feeds any reward math.
  - **Item/currency/pet drops and kill-count's small drop loop**: drops stay genuinely random server-side, tied to **whole kills crossed this window** (`wholeKillsThisWindow = floor(characterKillsBefore + expectedKillsThisWindow) - floor(characterKillsBefore)`, a small bounded loop over real kills rather than `totalAttacks`) so the existing drop/currency/pet-roll logic didn't need to change at all. Live-mode inventory-full still stops the window at the same point (`creditedFraction` prorates gold/EXP/kills down to whatever fraction of the whole-kill loop actually completed before running out of room).
  - **Kill counts are fractional now**: `character_monster_kills.kills`/`account_monster_kills.kills` widened from `integer` to `numeric` (migration `20260811020000_expected_value_kill_counts.sql`) so fractional per-resolve kill progress accumulates exactly instead of rounding to 0 almost every ~4s window — same "carry the remainder forward" idea `requiredExpForLevel`'s own loop already uses. `resolve_combat_apply_kill_counts`'s `p_kills_delta` is `numeric` too. Achievement tier-threshold comparisons (`claim_kill_count_reward`/`claim_account_achievement_reward`, `tierIndexReached` client-side) all work unchanged against fractional values; only the **rendered** "N kills" text floors it (`AchievementsPanel.tsx`).
  - **Predictive leveling**: `useProgressionStore` has `predictedLevel`/`predictedExp`/`predictedGold` — `addPredictedRewards` runs the same level-up loop as `addRewards`, so a kill crossing a level threshold shows the toast and bar rollover immediately. `ExpBar.tsx` reads `predictedLevel`/`predictedExp` directly (not added to confirmed — `predictedExp` IS the display value, reset to the confirmed value, not 0, on every sync, to avoid a flicker-to-zero bug). **Only the display fields are predictive — the real `level` field (attribute allotment, level-diff color, equip/shop gates) is untouched**, set only by `applyServerCombatResult`/`hydrate`/`addRewards`. `PREDICTED_EXP_SAFETY_FACTOR = 0.95` — predicted EXP gains are shaved 5% so a server correction is (almost) always an upward adjustment, never a visible drop; now just a small residual buffer for clock-skew rather than covering several kills' worth of RNG disagreement (see the expected-value rewrite above). **`addPredictedRewards` no longer floors per-call** (fixed 2026-08-11 alongside the rewrite — flooring every call would round the new small per-tick fractional deltas down to 0 and the bar would never move); `predictedExp` stays a genuine float, `predictedGold` is floored at render time instead (`ExpBar.tsx`'s `displayedGold`). `ExpBar.tsx` shows EXP as a percentage (`${percent.toFixed(2)}%`), not a fraction, so the float doesn't need flooring there. **Tiered gold display (2026-08-13)**: `formatGoldAmount`/`goldColorClass` (`src/game/stats/formatGold.ts`) replace the old flat `toLocaleString()`. Format: <1,000 a plain "Ng" number, 1,000-999,999 whole "Nk", 1,000,000-9,999,999 "N.NNM" (2 decimals), ≥10,000,000 whole "NM" (no decimals). Color: ≤100,000 gold (`text-amber-300`, unchanged), 100,000-9,999,999 white, ≥10,000,000 cyan. Scoped to `ExpBar.tsx`'s top-bar readout only — no other gold display (Shop, Bank, etc.) was changed. **Level Up banner moved to its own component (2026-08-20)**: the old version was `position: absolute` inside `ExpBar`'s own flex-item container (only as wide as that item, since the top HUD row is `flex flex-wrap` alongside several other HUD widgets), which the user reported sitting oddly. `LevelUpBanner.tsx` (mounted unconditionally in `GameShell`, still reading `lastLevelUp`/`clearLevelUpNotice` off `useProgressionStore`) portals to `document.body` at `z-[80]` instead — genuinely on top of everything, immune to any ancestor's stacking/overflow — plus a gold-only (`#FFD700`) confetti-ember burst around it (`EmberBurstPoint.tsx`, the same `buildConfettiEmbers` mechanics as `FireworkOverlay`/`MoneyBagRevealModal`, just scoped to a handful of points around the banner instead of one center point or scattered across the whole screen).
  - **"Loot Holding"**: a full Inventory at grant time during **offline** mode routes the drop into a ~100-slot `loot_holding` table (`LOOT_HOLDING_CAP` in `useLootHoldingStore.ts`) — one row per pending drop. Claiming: `claim_loot_holding(holding_id)` (ownership-verified, inserts real `item_instances` row + deletes holding row). If Loot Holding itself is full, further overflow is genuinely lost (extreme edge case). **Live play never uses Loot Holding** — see Combat's full-inventory-stops-combat note above.
  - `LootHoldingCard.tsx` (`src/components/LootHoldingCard.tsx`) lives **only inside `OfflineProgressModal.tsx`**, not a persistent Warehouse card. `sell_loot_holding(holding_id)` mirrors `sell_item`'s price formula. Currency-type entries have no template/price, Claim/Bank only.
  - **Staged UI (current, final)**: two stages derived purely from what's in `entries` (no separate wizard state) — was three stages that hid Normal-tier gear behind a single "Sell All Normal" button with no tiles shown at all (justified originally by how spammy Normal drops were); reworked 2026-08-10 (confirmed with the user, after the quality-drop-rate recalibration made any drop meaningfully rarer) so Normal gear gets the same individual-tile treatment quality gear already had. **(1) `currency`** — while any Comet/Fallen Star entries are present, shown as a tile grid with "Claim All"/"Store All" bulk actions. **(2) `gear`** — once currency is cleared, every remaining gear entry (Normal and quality alike) shows as an individual tile; tapping one opens a 3-action popover (**Claim**/**Store**/**Sell**) regardless of tier. A "Claim All" button (2026-08-13) sits above the grid, always available; a "Sell All Normal" button still appears above the grid whenever at least one Normal-tier item is present too, as a bulk-convenience shortcut, not a gate.
    - **"Claim All" only claims what currently fits (2026-08-13)**: both stages' "Claim All" call a shared `claimEntriesUpToRoom` helper (`LootHoldingCard.tsx`) that claims entries **sequentially** (`await` in a `for` loop, not `Promise.all`), stopping the moment Inventory hits `INVENTORY_SLOT_CAP` and leaving the rest in Loot Holding for later. Sequential, not parallel, on purpose — `claim_loot_holding` has no server-side room check at all (by design, see its own comment: the client-side pre-check is the only guard), and `claim()`'s pre-check reads whatever's in the Inventory store *at call time*; firing every claim in the batch at once would let them all pass that same pre-check before any of the earlier ones actually land, letting a batch over-claim past the cap. A local `bulkBusy` state (separate from the store's own `busy`, which flickers false between each sequential await) disables every bulk button for the duration of a batch.
    - **Store** (`store_loot_holding_to_bank(holding_id)`) inserts straight into `item_instances` with `location = 'bank'`, bypassing Inventory's 40-slot cap entirely — no server-side room check (client-side `useBankStore.addBankedItem` append), can only fail if Bank Storage's own 40-slot cap is full. Currency entries use the parallel `bank_loot_holding(holding_id)` RPC (routes into account-wide `players.bank_comets`/`bank_fallen_stars`, rejects gear with `'not_bankable'`).
    - Claim/Store buttons turn red (`TooltipActionPopover`'s `tone: 'warning'`) when their destination is full, computed from reactive raw-field Zustand selectors (not a non-reactive method call — see the Zustand selector gotcha in `CLAUDE.md`).
    - `OfflineProgressModal`'s bottom button is a plain "Got it" (doesn't force-loop Claim before closing — Store guarantees an always-available way to clear anything left).
  - **`UnclaimedLootBadge.tsx`**: a small fixed 🎁+count button, bottom-left (`bottom-20` mobile to clear the nav bar, `lg:bottom-4` desktop), shown whenever `useLootHoldingStore` has entries — tapping opens the modal in "Unclaimed rewards" mode (`useLootHoldingModalStore.openModal()`). This is the **one deliberate way** to reach leftover entries — the modal no longer passively auto-reopens just because entries exist.
  - **Bank withdraw over-cap fix**: `transfer_currency`'s Bank→character withdraw direction now caps at however many actually fit (mirrors `unbundle_currency_scroll`'s occupied-slot formula), returns `'not_enough_room'` + `max_withdrawable` if it doesn't fit. Gold unaffected (no tile cap).
  - **Level-appropriate drop selection**: `pickLevelAppropriateTemplate` (client, `useInventoryStore.ts`) and its `resolve-combat` mirror both pick a random gear family available to the class (excluding `'sword'`/`'quiver'`/`'lucky-bow'`), then the template in that family whose `required_level` is closest to the killed monster's level. Family choice is uniform-random (no rarity weighting within family) — **unresolved**.
  - **Drop rate + quality-on-drop**: gear drop chance **1/150** per kill (`DROP_CHANCE`, deliberately left untouched — see recalibration below). Given a drop, quality is a separate roll (`rollDroppedQualityTier`, rarest-first, first hit wins, else Normal): Tempered 3/32, Infused 1/16, Radiant 3/200, Ascended 3/400 (recalibrated 2026-08-10 from the original 1/500/1/2000/1/5000/1/15000, which the user reported as "very high" i.e. far too rare in practice). Grounded in `expCurve.ts`'s own ~1,200 kills/hour constant (a 2-hour AFK-cap session ≈ 2,400 kills) — target was 1-5 Tempered / a couple Infused / potentially a Radiant / an Ascended about once/day across ~4 sessions, then halved across the board as a deliberate safety margin before shipping, landing at ~1.5 Tempered / ~1 Infused / ~0.24 Radiant / ~0.12 Ascended expected per 2-hour session (~1 in 1,600 / 2,400 / 10,000 / 20,000 combined per-kill odds respectively). Both DROP_CHANCE and the quality roll scale up by `accountDropMultiplier` (see Achievements' account-wide bonus, `CLAUDE.achievements-and-pets.md`) — though in practice only the quality roll is affected, since the zone-bonus's own multiplier is applied there, not to DROP_CHANCE (see Account track note there). **Confirmed, deliberately unresolved**: no zone-level scaling of DROP_CHANCE itself yet (a level-120 zone doesn't drop more/better than Windhollow) — flagged as needed eventually, not decided.
  - **Composition +1 on drop (2026-08-12)**: layered on top of the quality-tier roll, not instead of it — a dropped item can land as e.g. Infused +1 at once. Independent roll, `rollDroppedCompositionLevel`, at the same conditional rate as the Infused quality roll above (currently 1/16, read directly off `QUALITY_DROP_CHANCES`' `'infused'` entry so the two can't drift out of sync), scaled by the same `accountDropMultiplier`. Applies to both the live `item_instances` insert and the offline `loot_holding` insert — `loot_holding` gained its own `composition_level integer not null default 0` column for this (previously always claimed in at +0; `claim_loot_holding`/`store_loot_holding_to_bank` now carry it through). Client mirror: none — `useInventoryStore.ts`'s own drop-chance copy is predictive-only combat-log flavor text and never rolled quality either, so it doesn't need this roll.
  - **Offline/idle catch-up always routes item + currency drops to Loot Holding**, never straight into Inventory, regardless of room. Live combat is unaffected (grants straight into Inventory, stops on full).

## Row Combat (Phase 1, v1.98.0)

A second, **live-only** combat mode alongside the single-target system above — 2 rows of 6 independently-toggled slots per character (12 total), each slot spawning whatever monster is currently selected in the normal Zone & Monster picker at the moment it's toggled on (not a fixed roster, so different slots can host different monster types simultaneously). Row slots attack the player back, client-side only (see below).

- **Ability/passive-only targets — no basic auto-attack (2026-08-17, requested by the user)**: row slots can only take damage from Multi-Shot (below) or future abilities/passives, never from the player's normal 1-attack-per-interval auto-attack. Originally the auto-attack DID target the first enabled+alive slot, same "no manual targeting" shape as single-target combat — removed because with 6 slots to pick off one at a time, plain auto-attack alone cleared Row 1 fast enough that Multi-Shot barely mattered. **Not mutually exclusive with single-target combat** — the normal Zone & Monster fight keeps running in the background exactly as always; toggling a row slot on was never actually wired to pause it (an earlier plan called for that handoff, it never got implemented, and that gap turned out to be exactly the wanted behavior: two independent damage sources, auto-attack on the normal target, abilities on row slots). Stored independently from single-target (`row_combat_last_resolved_at` vs `combat_last_resolved_at`) regardless.
- **No offline/idle path at all** — going AFK (tab hidden/`beforeunload`) must fully stop accrual, unlike single-target's multi-hour AFK cap. Enforced two ways: `RowCombatEngine.tsx` (sibling to `CombatEngine.tsx`, mounted alongside it in `GameShell.tsx`) fires the same visibility/beforeunload-triggered resolve, AND `resolve-row-combat`'s gather RPC applies a hard `ROW_LIVE_LIVENESS_THRESHOLD_MS` (10s) cutoff — any gap past that is zeroed. Matters less than it used to now that nothing accrues passively at all (see below), but still gates how stale a respawn can silently resolve.
- **Schema**: `characters.row_slots jsonb` (12-element array, index 0-5 = Row 1, 6-11 = Row 2; each `{enabled, monster_id, current_hp, max_hp, is_rare, dead_at}`), `row_combat_last_resolved_at`, `row_multi_shot_last_fired_at`, `row1_unlocked`/`row2_unlocked booleans`. All in `supabase/migrations/20260902000000_add_row_combat.sql`, unreachable from a plain client update (no column grant) — every mutation goes through a `security definer` RPC.
- **Reward math — no passive accrual at all, purely discrete actions**: since there's no auto-attack, a plain resolve call (`fireMultiShot: false`) only processes respawns that came due — no reward math runs at all. A `fireMultiShot: true` call applies one **real rolled hit** (not deterministic expected-value math) to every enabled+alive slot — see Multi-Shot's own bullet below. A kill is simply `current_hp` crossing ≤0, so overkill is naturally discarded and damage-dealt EXP (`DAMAGE_EXP_SHARE`) accrues per landed hit regardless of whether it kills. Genuine `Math.random()` calls (drops/quality/currency/pet/rare-flavor/respawn rarity) still happen per real kill, same odds tables as `resolve-combat` (`DROP_CHANCE`, `QUALITY_DROP_CHANCES`, `PET_DROP_CHANCE`, `JADE_SHARD_DROP_CHANCE`, etc. — all copied, must stay in sync with both `resolve-combat/index.ts` and `combatResolver.ts`). (Originally a bounded discrete-event walk also processed per-window auto-attack ticks — removed along with auto-attack itself; `applyHitToSlot` is now only ever called from the Multi-Shot branch.)
  - **Achievement + Gold Donation Event buffs both apply (Gold Donation Event fixed 2026-08-17, reported by the user as missing)**: `resolve_row_combat_gather_state` reads `account_zone_attack_bonus_pct`/`account_zone_drop_bonus_pct` (Achievements, was already correct) and now also `gold_donation_state`/`gold_donation_pools` for the event's active buff (was missing entirely — `resolve-row-combat/index.ts` had no `eventExpMultiplier`/`eventCometMultiplier`/`eventFallenStarMultiplier`/`eventQualityMultiplier` derivation at all before this fix, so a live event buff boosted single-target kills via `resolve-combat` but had zero effect on Multi-Shot). Same query and multiplier-derivation shape as `resolve-combat/index.ts`, added in `supabase/migrations/20260904020000_row_combat_gold_donation_event.sql`.
- **Multi-Shot**: Hunter-only, player-pressed (not automatic), fixed `MULTI_SHOT_COOLDOWN_MS` (10s placeholder, **aligned with `ROW_RESPAWN_MS`**, also 10s as of 2026-08-17 — was 15s, lowered to match) enforced server-side against `row_multi_shot_last_fired_at` — never trusts the client's own cooldown display. No mana/resource cost. No mechanical hook into the old, inert `promotion_tiers.skills_unlocked` "Multi-Shot" flavor text (Falcon Hunter tier 40, still unobtainable — see Accounts & Classes' Class Promotion section) — deliberately granted to all Hunters via a plain `class === 'hunter'` check instead, since gating it behind an unbuildable-until-Mining-ships promotion would block the whole mechanic.
  - **Requires a living target (2026-08-17, requested by the user)**: firing with every enabled slot empty/dead does NOT consume the cooldown (`multiShotNoTarget: true` in the response) — checked after `processRespawnsUpTo(now)`, so a slot whose respawn timer just elapsed still counts. Client mirrors this as a button-disable check (`useRowCombatStore`'s `slots.some(s => s.enabled && s.currentHp > 0)`) purely so the button doesn't invite a press the server would reject anyway — the server check is the real authority.
  - **Real rolled damage, 50% per target (2026-08-17, requested by the user)**: unlike single-target combat's deterministic expected-value reward math, Multi-Shot is a discrete, single, player-pressed action — closer to World Boss's "one real, immediately server-resolved attempt" than to a continuous per-window formula — so it rolls real damage per target: `resolvePhysicalDamage(rollDamageInRange(attackMidpoint), monsterDefense(...))` (same ±10% `DAMAGE_ROLL_MIN_RATIO`/`MAX_RATIO` variance every other attack in this game uses, newly added to `resolve-row-combat/index.ts` since single-target's own server function never needed a roll function), then **halved** (`MULTI_SHOT_DAMAGE_MULTIPLIER = 0.5`, applied to the final post-defense number) since it hits every target at once. Hit chance is also a real roll now (`Math.random() >= hitChance` → miss), not folded into an expected-value fraction. Real RNG here doesn't reintroduce the divergence problem the 2026-08-11 single-target rewrite fixed — that was about the *same* window being simulated twice (client tick vs. server), not about randomness existing; a discrete one-shot action only ever gets rolled once, server-side.
  - **Floating damage numbers**: the server returns `multiShotHits: {slotIndex, hit, damage}[]` in its response; `resolveRowCombat.ts` feeds this to `useRowCombatStore`'s `applyMultiShotHits`, and `RowCombatPanel.tsx`'s `RowSlotTile` renders a floating "-N"/"Miss" per hit slot (same `AnimatePresence`/`motion` visual language as `CombatPage.tsx`'s own floating numbers) — shown only once the server response actually arrives, never predicted client-side, since the damage is real RNG the client doesn't know in advance.
- **Monster attack-back is client-only**, mirroring single-target's own existing, documented gap ("player HP has never been simulated server-side") rather than introducing server-side player-HP simulation across up to 12 independent timers. Player HP/knockout is one shared pool across both modes — `useCombatStore.ts` gained `isKnockedOutAt(nowMs)`/`applyIncomingDamage(damage, nowMs, sourceName)` specifically so `useRowCombatStore`'s tick loop mutates the same HP state single-target's `runTick` does, instead of forking a second pool.
- **Client**: `useRowCombatStore.ts` (12-slot state; its tick loop only drives attack-back and the respawn countdown now — no auto-attack block), `resolveRowCombat.ts` (client wrapper, same "server response reconciles local state" convention as `resolveCombat.ts`), `RowCombatEngine.tsx`. Hydrated from `characters.row1_unlocked/row2_unlocked/row_slots` in `useCharacterRecordStore.ts`'s `loadCharacterRecord` (same place `useCombatStore.clear()` already resets on character switch — row state gets the same reset, to avoid the same "zombie fight" bug class).
- **RPCs**: `resolve_row_combat_gather_state`/`resolve_row_combat_apply_results`/`resolve_row_combat_release_claim` (service-role only, mirror `resolve_combat_*`'s exact-CAS claim/release shape), `toggle_row_slot` (security definer, ownership-checked, reads `selected_monster_id` server-side — never trusts a client-passed monster id; **client must always call `resolveRowCombat()` before toggling**, so pending time settles against pre-toggle slot state), `claim_row_unlock` (security definer, reuses the *existing* per-character Kill Count ladder — Row 1 at claimed tier 2/250 kills, Row 2 at tier 4/1000 kills, both placeholder/tunable — rather than a new achievement track), `clear_row_slots` (security definer, bulk-disables all 12 slots in one call — `supabase/migrations/20260903000000_clear_row_slots_on_target_switch.sql`).
- **Rows clear whenever the player switches which enemy they're fighting (2026-08-17, requested by the user)**: `CombatPage.tsx`'s `clearRowSlotsForCharacter` (optimistic local `applyServerSlots([])` + fire-and-forget `clear_row_slots` RPC) is called from `handleFight` — only on a genuine switch, guarded by `typeId !== monsterTypeId` so resuming the same monster (Stop → Resume) doesn't wipe active rows — and unconditionally from `handleSelectZone` (switching zones already clears single-target combat too). Otherwise Row Combat would keep fighting whatever monster type got locked into each slot at its own toggle-on time even after the player moved their primary target elsewhere.
- **UI**: `RowCombatPanel.tsx`, replacing `CombatPage.tsx`'s old 4-slot `MultiTargetSlots` placeholder (which was scaffolding for this exact feature). Locked-row unlock CTA modeled on `AchievementsPanel.tsx`'s claim-button pattern. Each tile: monster portrait (`portraitUrl`, or `hexColor(color)` swatch fallback — both exported from `CombatPage.tsx` for reuse, same as `HpBar`) centered filling most of the tile, HP bar pinned to the bottom, no visible name text (shown as a hover `title` instead) — 2026-08-17, requested by the user. Dead/respawning slots reuse `CombatPage.tsx`'s own `DeadOverlay` (also exported) — same dimmed-portrait + "Ns above Dead" treatment as the primary target's own respawn gap, via a `compact` prop that shrinks the text for the much smaller tile (the default sizing was tuned for the primary target's 128-160px portrait and would overflow a ~50-70px row tile).
- **Can't toggle a slot off mid-respawn (2026-08-17, requested by the user)**: without this, toggling a dead/respawning slot off (clears outright) then immediately back on (rolls a fresh full-HP spawn instantly) skipped the 10s `ROW_RESPAWN_MS` wait entirely. `toggle_row_slot` now checks `dead_at is not null` on the enabled→disable path and returns `error: 'respawning'` instead — `supabase/migrations/20260904010000_toggle_row_slot_blocks_respawning.sql`. Toggling off a still-*alive* slot is unaffected (no timer to bypass there). Client mirrors this as a disabled button during the respawn countdown, but the RPC is the real authority.
- **Edge Function**: new sibling `supabase/functions/resolve-row-combat/index.ts`, not a branch of `resolve-combat` — follows the same precedent `world-boss-attack/index.ts` set (new function when the state shape is different enough, accepting the resulting "known duplication" of attack/defense math). Gear durability decay is deliberately **not** simulated here (unlike `resolve-combat`) — windows are always ≤10s, negligible against the 18h-to-empty decay budget.
