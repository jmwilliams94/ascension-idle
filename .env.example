VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Public half of a VAPID keypair (`npx web-push generate-vapid-keys`) used for
# Web Push subscriptions -- safe to ship client-side. The private half is a
# Supabase Edge Function secret only (VAPID_PRIVATE_KEY), never here.
VITE_VAPID_PUBLIC_KEY=
